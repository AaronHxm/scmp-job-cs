# SCMP合同抢单系统配置文件

# API配置\api.base.url=http://api.example.com
api.timeout.ms=30000

# 抢单配置
grab.max.concurrent=5

# 调度器配置
scheduler.thread.pool.size=10

# 重试配置
retry.count=3
retry.interval.ms=1000

# 日志配置
log.level=INFO

# 应用标识
app.id=scmp-contract-grabber
app.version=1.0.0

# 开发环境配置
dev.mode=true